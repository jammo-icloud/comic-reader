A thin themed progress bar — accent fill on a track. Reading position, download progress, import progress.

```jsx
<ProgressBar value={62} />
```

4px tall, fully rounded, accent fill. Clamps `value` to 0–100. Pair with a tabular-nums label (e.g. `p.124`) for reading progress.
