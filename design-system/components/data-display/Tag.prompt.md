A small genre/metadata tag chip. Used in rows under cover titles and in tag-filter popovers.

```jsx
<Tag>action</Tag> <Tag>fantasy</Tag> <Tag>seinen</Tag>
```

Subtle by design — filled with `--bg-subtle`, tertiary text, capitalized. Keep tags to 2–3 visible per card.
