A text input. Default rounded field for forms and search; `comic` variant for the login spread's panel.

```jsx
<Input placeholder="Search your library" />
<Input variant="comic" placeholder="your username" />
```

Focus draws a 1px accent border + ring. The `comic` variant uses a 3px black border, square corners, and monospace — only for the login panel.
